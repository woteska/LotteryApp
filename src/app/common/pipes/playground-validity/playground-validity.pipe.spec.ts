import { PlaygroundValidityPipe } from './playground-validity.pipe';

describe('PlaygroundValidityPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaygroundValidityPipe();
    expect(pipe).toBeTruthy();
  });
});
